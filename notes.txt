This snippet can be used to convert modules

    @staticmethod
    def streamable_to_stationary(module: streamable.Module) -> torch.nn.Module:
        """Converts a streamable module to a stationary module.
        
        Inputs:
        - module: The streamable module.
        
        Outputs:
        - module: The stationary equivalent."""
            
        # Replace the call method
        stationary_type = type(module.split(".")[-1])
        stationary_forward = None
        # Try to get the stationary forward from torch
        try: 
            exec(f"stationary_forward = torch.nn.{stationary_type}")
        # Take it from the custom stationary modules
        except NameError:
            exec(f"stationary_forward = stationary.{stationary_type}")

        module.__call__ = stationary_forward

        # Outputs
        return module